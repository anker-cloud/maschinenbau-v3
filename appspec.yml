version: 0.0
os: linux
files:
  - source: /
    destination: /opt/sturtz/app
    overwrite: true
hooks:
  BeforeInstall:
    - location: scripts/codedeploy-stop.sh
      timeout: 60
      runas: root
  AfterInstall:
    - location: scripts/codedeploy-deploy.sh
      timeout: 900
      runas: root
